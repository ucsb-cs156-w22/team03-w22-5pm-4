Add endpoint to list earthquakes

# ACs

- [ ] In Swagger, there is an api endpoint `GET /api/earthquakes/all` accessible to all logged in users that lists all earthquakes in the earthquakes collection.


