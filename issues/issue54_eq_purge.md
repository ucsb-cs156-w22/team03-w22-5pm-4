Add endpoint to purge earthquakes

# ACs

- [ ] In Swagger, there is an api endpoint `POST /api/earthquakes/purge` accessible only to admins that deletes all earthquakes from the `earthquakes` collection. 



